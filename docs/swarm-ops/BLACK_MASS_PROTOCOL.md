# Black Mass Protocol (version line)

Operational doctrine for swarm mass movements. **Not** a graduation diploma.

| Version | Name | Purpose |
|---------|------|---------|
| **v0.5** | Black Mask | Pre-flight inspect — doctrine, hosts, JSONL, no fake ACK |
| **v1.5** | BlackMass | External swarm activation (Kimi payload, agents 001–300) |
| **v2.0** | BlackMass | Current — CF cloud LPM, autonomic flows, Main Brain roadmap gate |

## Receipts

Each milestone appends a **Main Brain** row via:

```bash
python scripts/kc_main_brain_roadmap.py receipt --milestone blackmass_v2_0
```

Kinds: `roadmap_seed_before`, `roadmap_black_mask_v0_5`, `roadmap_blackmass_v1_5`, `roadmap_blackmass_v2_0`, `roadmap_seed_after`.

## Agent 003 (Teacher)

Validates student findings against Commandments + this protocol — see [PAYLOAD_KIMI_300_ACTIVATION.md](./PAYLOAD_KIMI_300_ACTIVATION.md).
